import { describe, it } from "vitest";

describe("Playlist", () => {
  it("Fails to create a playlist with no session", () => {});
  it("Fails to create a playlist if image is too large", () => {});
  it("Fails to create a playlist if title is too short", () => {});
  it("Fails to create a playlist if title is too long", () => {});
  it("Fails to create a playlist if description is too long", () => {});
  it("Fails to create a playlist if there are too many tags", () => {});
  it("Create a playlist sucessfully", () => {});
});
